<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TeamProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

        <item name="colorTitleBar">@color/titleBar</item>
        <item name="colorTitleBarText">@color/white</item>

        <item name="colorDisabledButton">@color/whiteGray</item>
        <item name="colorDisabledButtonText">@color/white</item>
        <item name="colorEnabledButton">@color/primary</item>
        <item name="colorEnabledButtonText">@color/white</item>
        <item name="colorPressedButton">@color/primaryMoreDark</item>
        <item name="colorPressedButtonText">@color/white</item>
    </style>

    <style name="Theme.TeamProject" parent="Base.Theme.TeamProject" />
</resources>